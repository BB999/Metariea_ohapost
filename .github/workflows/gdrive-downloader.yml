name: Google Drive Downloader

on:
  workflow_call:
    inputs:
      gdrive-base-path:
        description: 'Google Driveのベースパス'
        required: true
        type: string
      artifact-name:
        description: 'アーティファクト名'
        required: false
        type: string
        default: 'gdrive-images'
    outputs:
      folder-name:
        description: '取得したフォルダ名'
        value: ${{ jobs.download-images.outputs.folder-name }}
      next-number:
        description: '次の番号'
        value: ${{ jobs.download-images.outputs.next-number }}
      image-name:
        description: '選択した画像名'
        value: ${{ jobs.download-images.outputs.image-name }}
      folder-path:
        description: '画像のあるフォルダパス'
        value: ${{ jobs.download-images.outputs.folder-path }}

permissions:
  contents: write

jobs:
  download-images:
    runs-on: ubuntu-latest
    outputs:
      folder-name: ${{ steps.get-folder.outputs.folder-name }}
      next-number: ${{ steps.get-folder.outputs.next-number }}
      image-name: ${{ steps.select-image.outputs.image-name }}
      folder-path: ${{ steps.select-image.outputs.folder-path }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install rclone
      run: |
        curl https://rclone.org/install.sh | sudo bash

    - name: Setup rclone config
      run: |
        mkdir -p ~/.config/rclone
        echo "${{ secrets.RCLONE_CONF_BASE64 }}" | base64 -d > ~/.config/rclone/rclone.conf

    - name: Get next folder
      id: get-folder
      run: |
        # 前回の番号を取得
        PREV_NUM=$(cat "previous file")
        echo "Previous number: $PREV_NUM"

        # フォルダの最大数を取得
        MAX_NUM=$(rclone lsd "${{ inputs.gdrive-base-path }}" | wc -l | tr -d ' ')
        echo "Max folders: $MAX_NUM"

        # 次の番号（最大数を超えたら1に戻す）
        NEXT_NUM=$((PREV_NUM + 1))
        if [ $NEXT_NUM -gt $MAX_NUM ]; then
          NEXT_NUM=1
          echo "Reached max, resetting to 1"
        fi
        echo "Next number: $NEXT_NUM"

        # フォルダ一覧を取得して次の番号のフォルダを探す
        FOLDER=$(rclone lsd "${{ inputs.gdrive-base-path }}" | awk '{print $NF}' | grep "^${NEXT_NUM}:" | head -n 1)

        if [ -z "$FOLDER" ]; then
          echo "❌ Folder with number $NEXT_NUM not found"
          exit 1
        fi

        echo "📁 Found folder: $FOLDER"
        {
          echo "folder-name<<EOF"
          echo "$FOLDER"
          echo "EOF"
          echo "next-number=$NEXT_NUM"
        } >> $GITHUB_OUTPUT

    - name: Select and download random image
      id: select-image
      run: |
        mkdir -p ./images
        FOLDER="${{ steps.get-folder.outputs.folder-name }}"

        # フォルダ内のファイル一覧を取得してランダムに1つ選択
        IMAGE=$(rclone ls "${{ inputs.gdrive-base-path }}$FOLDER/" | awk '{print $2}' | shuf -n 1)

        if [ -z "$IMAGE" ]; then
          echo "❌ No image found in folder"
          exit 1
        fi

        echo "📷 Selected image: $IMAGE"

        # 選択した画像だけダウンロード
        rclone copy "${{ inputs.gdrive-base-path }}$FOLDER/$IMAGE" ./images/

        echo "📁 Downloaded file:"
        ls -la ./images/

        # 出力
        {
          echo "image-name<<EOF"
          echo "$IMAGE"
          echo "EOF"
          echo "folder-path<<EOF2"
          echo "${{ inputs.gdrive-base-path }}$FOLDER/"
          echo "EOF2"
        } >> $GITHUB_OUTPUT

    - name: Upload as artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.artifact-name }}
        path: ./images/
